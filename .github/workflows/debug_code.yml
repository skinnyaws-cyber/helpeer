name: Code Scanner 🔍

on:
  workflow_dispatch:

jobs:
  check-code:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: '8.5' # نحدد إصداراً حديثاً ومتوافقاً

    # هنا التغيير الجوهري: نستخدم gradle مباشرة بدلاً من ./gradlew
    - name: Scan Kotlin Code
      run: gradle compileDebugKotlin --stacktrace
